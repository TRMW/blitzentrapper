# Copy to .env and set values. .env is gitignored.
# For local dev pointed at a copy of production, see the README "Local development" section
# (it walks you through dumping the Railway Postgres and restoring into a local DB).
DATABASE_URL=
