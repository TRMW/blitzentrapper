# Copy to .env and set values. .env is gitignored.
# Set DATABASE_URL to a Postgres connection string for local development.
# Leave blank to fall back to local Postgres (blitzen_db_development).
DATABASE_URL=
